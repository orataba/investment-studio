[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "portfolio-ops-instrument-core"
version = "0.1.0"
description = "Shared instrument core contracts for Portfolio Operations Workbench"
requires-python = ">=3.12"
dependencies = [
  "pydantic>=2.10,<3.0",
  "sqlalchemy>=2.0,<3.0",
]

[tool.setuptools.packages.find]
include = ["portfolio_ops_instrument_core*"]
